language: php

php:
  - 5.3
  - 5.4
  - 5.5
  - 5.6
  - hhvm

before_script:
  - composer self-update
  - composer install --no-interaction

script: phpunit

cache:
  directories:
  - $HOME/.composer/cache/

sudo: false

git:
  depth: 5
